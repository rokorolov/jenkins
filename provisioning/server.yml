---
-   name: Set Up manager
    hosts: manager
    remote_user: root
    roles:
        - swap
        - docker
        -   role: docker-cache
            vars:
                config_path: /etc/docker/dind-daemon.json
        - jenkins
